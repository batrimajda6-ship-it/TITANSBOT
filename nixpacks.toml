[phases.setup]
nixPkgs = ["libsodium", "pkg-config", "ffmpeg"]

[phases.install]
cmds = ["SODIUM_INSTALL=system pip install --force-reinstall --prefer-binary PyNaCl==1.5.0", "pip install --prefer-binary davey"]
